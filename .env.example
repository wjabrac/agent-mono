# Copy to .env and replace each value with a strong random string.
# To generate all secrets at once, run ./docker/gen-env.sh
GRAFANA_ADMIN_PASSWORD=changeme
N8N_USER=admin
N8N_PASS=changeme
MEILI_MASTER_KEY=changeme
QDRANT_API_KEY=changeme
NEXTAUTH_SECRET=changeme
LANGFUSE_SALT=changeme
WORDPRESS_JWT_AUTH_SECRET_KEY=changeme
